/*
var r1 = require ('readline');
var fs = require ('fs');
var arr = [];
var user_input= process.stdin;
var prompts = r1.createInterface(process.stdin, process.stdout);
prompts.question("How many filenames you want to enter ?", function(fname));
for (var i = 0 ;i<=fname; i++)
{
    console.log('Enter the filename');
    user_input.on('data', function(data))
    arr.push(data)
}
arr[i]= filename;

fs.writeFile("fs");
wrtefile(arr[i[)

]])

if (filename = filename)
console.log ("enter another file name.this file name already exists")
fs.writeFile('', you are awesome)

add to Array





var arrfile = [];
function addFile

*/
